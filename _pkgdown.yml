url: https://soodoku.github.io/tubern

template:
  params:
    bootswatch: cosmo
    docsearch:
      api_key: 1f3c1636050e97a5149c7b98308d602a
      index_name: tubern

authors:
  Gaurav Sood:
    href: https://github.com/soodoku

# No `reference:` section on purpose. The hand-written index listed 8 topics
# and pkgdown found 29 more it did not cover, so the site failed to build at
# all. An index you must remember to update every time you add a function is
# the same drift problem this fleet standard exists to remove, one scale down.
# Without the section pkgdown lists every exported topic and cannot fall
# behind. Curated groupings are welcome back if they are kept complete.
